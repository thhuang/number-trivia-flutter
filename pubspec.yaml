name: number_trivia
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # Provider for service locator and state management
  provider: ^3.1.0+1
  # Value equality
  equatable: ^0.6.1
  # Functional programming
  dartz: ^0.8.8
  # Remote API
  data_connection_checker: ^0.3.4
  http: ^0.12.0+2
  # Local cache
  shared_preferences: ^0.5.4+3
  # Text resizing
  auto_size_text: ^2.1.0
  # Loading indicators
  flutter_spinkit: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1

flutter:
  uses-material-design: true
